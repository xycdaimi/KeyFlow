# 项目闭环评估待办

> 更新时间：2026-03-19

## 仍需补齐

- 补 `Redis + Lua` 真实并发分配测试，确认不会重复分配同一 key。
- 补 `PostgreSQL` 持久化链路验证，确认真实仓储路径可用。
- 补 `/health` 接口回归或端到端验证。
- 补 Docker 本地联调验证，确认 `.env.example` 与部署件可直接驱动运行。
- 若要继续推进结构化凭证重构，需补数据库迁移策略与线上/现有库切换方案；当前代码已按 `break-now` 口径切换，不保留旧 `api_key` 字符串兼容。
- 明确“worker”口径。若接受当前实现，文档应统一为“cooldown 内联恢复，无独立 worker 程序入口”；若必须保留独立 worker，则需新增对应程序入口、测试与部署说明。
- 处理 `tests/test_gemini_webapi.py`：该文件当前含硬编码凭证并在导入阶段直接访问外部服务，需要改为手工脚本、受控集成测试，或从默认 `pytest` 套件中隔离。

## 建议顺序

1. 先做 Redis + Lua 并发验证
2. 再做 PostgreSQL / health / Docker 联调
3. 最后统一 worker 口径并收口剩余文档
