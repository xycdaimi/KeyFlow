# KeyFlow 交接文档

这份文档只写接手必须知道的内容。接手人先按本文跑通部署和检查，再去看源码细节。

## 1. 先看这几个文件

| 优先级 | 文件 | 用途 |
| --- | --- | --- |
| 必看 | `docs/镜像部署.md` | 生产镜像 tar 分离部署说明，包含 gateway/node 两类服务器、上传哪些文件、启动脚本、端口、重启、日志检查。 |
| 必看 | `.env.gateway.example` | Gateway 控制面配置模板。生产 `.env.gateway` 必须按这个模板维护。 |
| 必看 | `.env.example` | 子节点配置模板。当前推荐把 `KEYFLOW_RUNTIME_MODE` 改成 `local`。 |
| 必看 | `config/model_aliases.example.yaml` | 模型别名配置模板。生产一般需要复制为 `config/model_aliases.yaml`。 |
| 必看 | `scripts/start_gateway_image_runtime.sh` | Gateway 镜像 tar 运行脚本。 |
| 必看 | `scripts/start_node_image_runtime.sh` | 子节点镜像 tar 运行脚本，会同时启动 `keyflow-api` 和 `keyflow-worker`。 |
| 参考 | `docs/plugin.md` | provider 插件说明。 |
| 参考 | `docs/router.md`、`docs/gateway-router.md` | API 路由和 Gateway 路由说明。 |

重点：生产部署不要只看 README，要优先看 `docs/镜像部署.md` 和本文第 4 节。

## 2. 当前推荐部署方式

当前推荐：

```env
KEYFLOW_RUNTIME_MODE=local
```

也就是子节点使用 SQLite WAL，不再默认依赖 PostgreSQL + Redis。原因是当前规模下 `dev` 模式太重，运维成本高，交接和部署都容易出错。

生产子节点 `.env` 建议写：

```env
KEYFLOW_RUNTIME_MODE=local
LOCAL_SQLITE_PATH=/data/keyflow.db
```

为什么是 `/data/keyflow.db`：

- `scripts/start_node_image_runtime.sh` 会创建 Docker volume：`keyflow-node-data`。
- 这个 volume 会挂载到容器 `/data`。
- SQLite 数据文件放在 `/data/keyflow.db`，容器删除重建后数据仍在 volume 中。

`dev` 模式仍保留，但只在确实需要 PostgreSQL + Redis 时使用：

```env
KEYFLOW_RUNTIME_MODE=dev
DATABASE_URL_READ=postgresql+asyncpg://...
DATABASE_URL_WRITE=postgresql+asyncpg://...
REDIS_URL=redis://...
```

注意：`docs/镜像部署.md` 里“子节点数据库和 Redis”那一节是历史 dev 模式写法。当前交接以本文的 local 推荐模式为准。

## 3. 服务拆分

生产分两类服务器、两个镜像 tar。

Gateway 调度服务器：

- 镜像：`keyflow-gateway:prod`
- tar 包：`dist/keyflow-gateway-prod.tar`
- 容器：`keyflow-gateway`
- 配置：`.env.gateway`
- 入口：`gateway.main:app`
- 职责：节点注册、心跳、节点能力汇总、管理请求转发

子节点服务器：

- 镜像：`keyflow-node:prod`
- tar 包：`dist/keyflow-node-prod.tar`
- 容器：`keyflow-api`、`keyflow-worker`
- 配置：`.env`、`config/`
- API 入口：`main:app`
- worker 入口：`python src/worker_main.py`
- 职责：保存 key、分配 key、接收成功/失败上报、刷新 key 状态

启动顺序：

1. 先启动 Gateway。
2. 再启动每台子节点。
3. 子节点启动后会主动向 Gateway 注册并发送心跳。

## 4. 生产部署流程

详细部署步骤以 `docs/镜像部署.md` 为主。这里写交接时最常用的最短流程。

### 4.1 开发机打包

Linux / macOS：

```bash
./scripts/build_gateway_image_tar.sh
./scripts/build_node_image_tar.sh
```

Windows：

```bat
scripts\build_gateway_image_tar.bat
scripts\build_node_image_tar.bat
```

默认产物：

```text
dist/keyflow-gateway-prod.tar
dist/keyflow-node-prod.tar
```

如果要指定版本标签：

```bash
KEYFLOW_IMAGE_TAG=2026-06-16 ./scripts/build_gateway_image_tar.sh
KEYFLOW_IMAGE_TAG=2026-06-16 ./scripts/build_node_image_tar.sh
```

### 4.2 Gateway 服务器部署

上传到 Gateway 服务器：

```text
dist/keyflow-gateway-prod.tar
.env.gateway
scripts/start_gateway_image_runtime.sh
```

启动：

```bash
chmod +x scripts/start_gateway_image_runtime.sh
./scripts/start_gateway_image_runtime.sh --load dist/keyflow-gateway-prod.tar
```

指定宿主机端口：

```bash
./scripts/start_gateway_image_runtime.sh --load dist/keyflow-gateway-prod.tar --port 18001
```

替换旧容器：

```bash
./scripts/start_gateway_image_runtime.sh --load dist/keyflow-gateway-prod.tar --replace
```

修改 `.env.gateway` 后：

```bash
docker restart keyflow-gateway
```

修改端口后必须重建容器：

```bash
./scripts/start_gateway_image_runtime.sh --load dist/keyflow-gateway-prod.tar --port 18001 --replace
```

### 4.3 子节点服务器部署

上传到每台子节点服务器：

```text
dist/keyflow-node-prod.tar
.env
config/
scripts/start_node_image_runtime.sh
```

子节点 `.env` 至少确认：

```env
PORT=8000
INTERNAL_API_KEY=生产内部密钥
MODEL_ALIAS_CONFIG_PATH=config/model_aliases.yaml
KEYFLOW_RUNTIME_MODE=local
LOCAL_SQLITE_PATH=/data/keyflow.db
```

如果要注册到 Gateway，还要确认：

```env
GATEWAY_URL=http://gateway-server:8001
GATEWAY_REGISTER_KEY=必须和 .env.gateway 一致
NODE_ID=node-shanghai-01
NODE_DISPLAY_NAME=Shanghai Node 01
NODE_PUBLIC_BASE_URL=http://node-server:18000
NODE_TAGS=shanghai,telecom
NODE_HEARTBEAT_INTERVAL_SECONDS=30
```

启动：

```bash
chmod +x scripts/start_node_image_runtime.sh
./scripts/start_node_image_runtime.sh --load dist/keyflow-node-prod.tar
```

指定宿主机端口：

```bash
./scripts/start_node_image_runtime.sh --load dist/keyflow-node-prod.tar --port 18000
```

替换旧容器：

```bash
./scripts/start_node_image_runtime.sh --load dist/keyflow-node-prod.tar --replace
```

修改 `.env` 或 `config/` 后：

```bash
docker restart keyflow-api keyflow-worker
```

修改端口后必须重建容器：

```bash
./scripts/start_node_image_runtime.sh --load dist/keyflow-node-prod.tar --port 18000 --replace
```

## 5. 生产检查命令

Gateway 服务器：

```bash
docker ps --filter "name=keyflow-gateway"
curl http://127.0.0.1:8001/openapi.json
docker logs --tail 100 keyflow-gateway
```

如果 Gateway 对外端口不是 8001，把 curl 里的端口换成实际 `--port`。

子节点服务器：

```bash
docker ps --filter "name=keyflow-"
curl http://127.0.0.1:8000/health
docker logs --tail 100 keyflow-api
docker logs --tail 100 keyflow-worker
```

如果子节点对外端口不是 8000，把 curl 里的端口换成实际 `--port`。

Gateway 查看节点：

```bash
curl -H "X-Gateway-Internal-Key: 生产网关管理密钥" \
  http://gateway-server:8001/api/gateway/nodes
```

Gateway 查看节点能力：

```bash
curl -H "X-Gateway-Internal-Key: 生产网关管理密钥" \
  http://gateway-server:8001/api/gateway/capabilities
```

## 6. 配置文件重点

### 6.1 `.env.gateway`

Gateway 必须重点确认：

```env
PORT=8001
API_PREFIX=/api/gateway
KEYFLOW_RUNTIME_MODE=local
LOCAL_SQLITE_PATH=data/keyflow_gateway.db
GATEWAY_INTERNAL_KEY=生产网关管理密钥
GATEWAY_REGISTER_KEY=生产节点注册密钥
GATEWAY_HEARTBEAT_TIMEOUT_SECONDS=90
```

字段说明：

- `GATEWAY_INTERNAL_KEY`：调用 Gateway 管理接口时用，请求头是 `X-Gateway-Internal-Key`。
- `GATEWAY_REGISTER_KEY`：子节点注册和心跳时用，请求头是 `X-Gateway-Register-Key`。
- `LOCAL_SQLITE_PATH`：Gateway 保存节点信息的 SQLite 文件。镜像部署脚本会挂载 `keyflow-gateway-data` 到 `/app/data`。

### 6.2 子节点 `.env`

子节点必须重点确认：

```env
PORT=8000
API_PREFIX=/api
INTERNAL_API_KEY=生产子节点内部密钥
MODEL_ALIAS_CONFIG_PATH=config/model_aliases.yaml
KEYFLOW_RUNTIME_MODE=local
LOCAL_SQLITE_PATH=/data/keyflow.db
UVICORN_WORKERS=1
BACKGROUND_TASK_INTERVAL_SECONDS=60
```

字段说明：

- `INTERNAL_API_KEY`：调用子节点管理接口和分配接口时用，请求头是 `X-Internal-Key`。
- `MODEL_ALIAS_CONFIG_PATH`：模型别名配置路径，容器里通常是 `config/model_aliases.yaml`。
- `LOCAL_SQLITE_PATH`：当前推荐 `/data/keyflow.db`。
- `BACKGROUND_TASK_INTERVAL_SECONDS`：worker 后台刷新间隔。

### 6.3 `config/model_aliases.yaml`

从模板复制：

```bash
cp config/model_aliases.example.yaml config/model_aliases.yaml
```

用途：

- 上游按统一模型名请求。
- KeyFlow 根据这个文件映射到具体 provider 和 provider 原生模型名。
- 分配响应里的 `provider_model` 要给上游真实调用 provider 时使用。

修改后重启：

```bash
docker restart keyflow-api keyflow-worker
```

## 7. 日常操作

查看日志：

```bash
docker logs --tail 100 keyflow-gateway
docker logs --tail 100 keyflow-api
docker logs --tail 100 keyflow-worker
```

持续跟日志：

```bash
docker logs -f keyflow-api
docker logs -f keyflow-worker
```

重启：

```bash
docker restart keyflow-gateway
docker restart keyflow-api keyflow-worker
```

停止并删除容器：

```bash
docker stop keyflow-gateway
docker rm keyflow-gateway
docker stop keyflow-worker keyflow-api
docker rm keyflow-worker keyflow-api
```

查看 SQLite 数据 volume：

```bash
docker volume ls | grep keyflow
docker inspect keyflow-api --format '{{json .Mounts}}'
docker inspect keyflow-gateway --format '{{json .Mounts}}'
```

日志轮转由启动脚本默认配置：

```text
--log-driver local
--log-opt max-size=100m
--log-opt max-file=3
```

需要调整时，在启动脚本前加环境变量：

```bash
KEYFLOW_LOG_MAX_SIZE=200m KEYFLOW_LOG_MAX_FILE=5 \
  ./scripts/start_node_image_runtime.sh --load dist/keyflow-node-prod.tar --replace
```

## 8. 核心接口

### 8.1 子节点健康检查

```bash
curl http://node-server:8000/health
```

local 模式下主要检查 app 和 SQLite。

### 8.2 查看 provider 列表

```bash
curl -H "X-Internal-Key: 子节点内部密钥" \
  http://node-server:8000/api/providers
```

这个接口用来确认 provider 名称、凭证格式、插件是否可用。

### 8.3 新增 key

```bash
curl -X POST http://node-server:8000/api/providers/openai/keys \
  -H "Content-Type: application/json" \
  -H "X-Internal-Key: 子节点内部密钥" \
  -d '{
    "credential": {
      "api_key": "sk-xxx"
    },
    "pool": "default",
    "max_concurrent_uses": 1
  }'
```

不同 provider 的 `credential` 结构不同，先看 `/api/providers` 的 `credential_hint`。

### 8.4 按 provider 分配 key

```bash
curl -X POST http://node-server:8000/api/internal/allocate-key \
  -H "Content-Type: application/json" \
  -H "X-Internal-Key: 子节点内部密钥" \
  -d '{
    "provider": "openai",
    "model": "gpt-4o",
    "pool": "default",
    "lease_seconds": 2
  }'
```

### 8.5 按模型分配 key

```bash
curl -X POST http://node-server:8000/api/internal/allocate-by-model \
  -H "Content-Type: application/json" \
  -H "X-Internal-Key: 子节点内部密钥" \
  -d '{
    "model": "gpt-4o",
    "pool": "default",
    "lease_seconds": 2
  }'
```

成功返回里最重要的是：

```json
{
  "key_id": "...",
  "lease_id": "...",
  "provider": "...",
  "provider_model": "...",
  "credential": {}
}
```

调用方必须保存 `key_id` 和 `lease_id`，后续上报要用。

### 8.6 成功上报

```bash
curl -X POST http://node-server:8000/api/internal/report-success \
  -H "Content-Type: application/json" \
  -H "X-Internal-Key: 子节点内部密钥" \
  -d '{
    "key_id": "分配返回的 key_id",
    "lease_id": "分配返回的 lease_id",
    "tokens_used": 123
  }'
```

### 8.7 失败上报

```bash
curl -X POST http://node-server:8000/api/internal/report-error \
  -H "Content-Type: application/json" \
  -H "X-Internal-Key: 子节点内部密钥" \
  -d '{
    "key_id": "分配返回的 key_id",
    "lease_id": "分配返回的 lease_id",
    "error_type": "rate_limit"
  }'
```

## 9. 常见问题排查

### 9.1 Gateway 看不到子节点

优先检查子节点 `.env`：

```env
GATEWAY_URL=
GATEWAY_REGISTER_KEY=
NODE_ID=
NODE_DISPLAY_NAME=
NODE_PUBLIC_BASE_URL=
```

重点：

- `GATEWAY_REGISTER_KEY` 必须和 `.env.gateway` 中的值一致。
- `NODE_PUBLIC_BASE_URL` 必须是 Gateway 能访问到的子节点 API 地址。
- 如果子节点启动早于 Gateway，重启 `keyflow-api` 触发重新注册。

命令：

```bash
docker logs --tail 100 keyflow-api
docker logs --tail 100 keyflow-gateway
```

### 9.2 `/health` 返回 503

local 模式重点查：

- `.env` 里的 `LOCAL_SQLITE_PATH` 是否是 `/data/keyflow.db`。
- `keyflow-api` 是否挂载了 `keyflow-node-data:/data`。
- 容器是否有权限写 `/data`。
- 磁盘是否满了。

命令：

```bash
docker logs --tail 200 keyflow-api
docker inspect keyflow-api --format '{{json .Mounts}}'
df -h
```

### 9.3 分配返回 `no_available_key`

常见原因：

- 没录入该 provider 的 key。
- key 被禁用。
- key 正在冷却。
- key 达到并发上限。
- 请求的 `pool` 不对。
- 按模型分配时，`config/model_aliases.yaml` 没配置该模型。

排查：

```bash
curl -H "X-Internal-Key: 子节点内部密钥" http://node-server:8000/api/providers
curl -H "X-Internal-Key: 子节点内部密钥" http://node-server:8000/api/providers/openai/keys
```

### 9.4 修改端口后不生效

端口分两层：

- `.env` / `.env.gateway` 里的 `PORT` 是容器内应用监听端口。
- 启动脚本 `--port` 是宿主机暴露端口。

Docker 端口映射在容器创建时确定。修改端口后必须 `--replace` 重建容器。

### 9.5 修改 `.env` 或 `config/` 后不生效

普通配置修改只需要重启：

```bash
docker restart keyflow-api keyflow-worker
docker restart keyflow-gateway
```

端口修改例外，需要 `--replace`。

## 10. 代码入口

最常看的代码文件：

```text
src/main.py
src/worker_main.py
src/gateway/main.py
src/gateway/app.py
src/interfaces/api/app.py
src/interfaces/api/routes/admin.py
src/interfaces/api/routes/allocate.py
src/interfaces/api/routes/report.py
src/interfaces/api/routes/health.py
src/application/services/key_service.py
src/domain/services/scheduler.py
src/domain/services/scorer.py
src/domain/services/state_machine.py
src/infrastructure/config/settings.py
src/infrastructure/plugins/base.py
src/infrastructure/plugins/providers/
src/container/container.py
```

分层规则：

- `interfaces/`：HTTP 接口、请求响应模型。
- `application/`：用例编排。
- `domain/`：调度、评分、状态机等核心规则。
- `infrastructure/`：SQLite、PostgreSQL、Redis、配置、插件。
- `gateway/`：Gateway 控制面。
- `container/`：依赖注入和插件注册。

## 11. 接手时必须拿到的信息

必须从离职交接里拿到：

- Gateway 服务器地址、登录方式、对外端口。
- 每台子节点服务器地址、登录方式、对外端口。
- 每台子节点的 `NODE_ID`、`NODE_DISPLAY_NAME`、`NODE_PUBLIC_BASE_URL`。
- 生产 `.env.gateway`。
- 每台子节点生产 `.env`。
- 生产 `config/model_aliases.yaml`。
- SQLite 数据 volume 名称和备份方式，默认关注 `keyflow-node-data`、`keyflow-gateway-data`。
- Gateway 管理密钥 `GATEWAY_INTERNAL_KEY`。
- 子节点注册密钥 `GATEWAY_REGISTER_KEY`。
- 子节点内部密钥 `INTERNAL_API_KEY`。
- 各 provider 的 key 来源、更新人、更新流程。
- 上游哪个服务调用 KeyFlow，负责人是谁，使用哪个接口。
- 发版流程：谁打包、谁上传 tar、谁执行 `--replace`。
- 日志和告警在哪里看。

不要把真实 `.env`、真实 token、真实 provider credential 提交到 git。

## 12. 接手验收清单

新接手的人至少要自己完成一遍：

1. 找到并读完 `docs/镜像部署.md`。
2. 确认生产推荐模式是 `KEYFLOW_RUNTIME_MODE=local`。
3. 知道 Gateway 服务器和子节点服务器分别跑什么容器。
4. 能打出 `dist/keyflow-gateway-prod.tar` 和 `dist/keyflow-node-prod.tar`。
5. 能用 `start_gateway_image_runtime.sh` 启动或替换 Gateway。
6. 能用 `start_node_image_runtime.sh` 启动或替换子节点。
7. 能检查 `keyflow-gateway`、`keyflow-api`、`keyflow-worker` 日志。
8. 能访问子节点 `/health`。
9. 能通过 Gateway `/api/gateway/nodes` 看到子节点。
10. 能调用 `/api/providers` 看 provider 列表。
11. 能新增一个测试 key。
12. 能调用分配接口拿到 `key_id` 和 `lease_id`。
13. 能成功上报 success 或 error。
